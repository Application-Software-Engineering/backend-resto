services:
  api-resto:
    container_name: nodejs_sqlite_app
    
    image: thisnoir/backend-resto:latest
    
    ports:
      - "3001:3000"
      
    volumes:
      - ./data:/app/data

    env_file:
      - .env
      
    restart: unless-stopped
